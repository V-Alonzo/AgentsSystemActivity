name: AgentsSystem
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - gradio
      - python-dotenv
      - pydantic
      - openpyxl
      - xlrd
      - openai-agents
